apiVersion: apps/v1
kind: Deployment
metadata:
  name: discordsoundboard-deployment-backend
  labels:
    app: discordsoundboard
    tier: backend
spec:
  selector:
    matchLabels:
      app: discordsoundboard
      tier: backend
  template:
    metadata:
      labels:
        app: discordsoundboard
        tier: backend
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: discordsoundboard
          image: ghcr.io/ggresillion/discordsoundboard_backend:1.0.4
          ports:
            - containerPort: 8080
          env:
            - name: BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: discordsoundboard-application-secrets
                  key: botToken
            - name: CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: discordsoundboard-application-secrets
                  key: clientId
            - name: CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: discordsoundboard-application-secrets
                  key: clientSecret
            - name: SPOTIFY_ID
              valueFrom:
                secretKeyRef:
                  name: discordsoundboard-application-secrets
                  key: spotifyId
            - name: SPOTIFY_SECRET
              valueFrom:
                secretKeyRef:
                  name: discordsoundboard-application-secrets
                  key: spotifySecret