{
  "name": "discordsoundboard",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon src/server/Server.js --exec babel-node",
    "start_prod": "npm run build && forever start dist/server/Server.js",
    "install_prod": "npm install & (cd web-client && npm install)",
    "build": "babel src/ -d dist/ & npm run build_web_client",
    "build_web_client": "cd web-client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_with_web_client": "npm start & npm run start_web_client",
    "start_web_client": "cd web-client && npm start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-preset-env": "^1.6.1",
    "discord.js": "^11.3.2",
    "express": "^4.16.3",
    "forever": "^0.15.3",
    "fs": "0.0.1-security",
    "node-opus": "^0.2.7",
    "semantic-ui-css": "^2.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1",
    "nodemon": "^1.17.1"
  }
}
