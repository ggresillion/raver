deploy:
  - okteto build -t discordsoundboard/api:${OKTETO_GIT_COMMIT} .
  - okteto build -t discordsoundboard/client:${OKTETO_GIT_COMMIT} client
  - helm upgrade --install discordsoundboard chart --set tag=${OKTETO_GIT_COMMIT}
devs:
  - okteto.yml
  - client/okteto.yml
