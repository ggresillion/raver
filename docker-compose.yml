version: "2.0"

services:

  api:
    build: .

  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - app-volume:/var/www/
    ports:
      - "80:80"

  front:
    build: ./web-client/
    volumes:
      - app-volume:/var/www/

volumes:
    app-volume: